React Frontend
- User uploads image
- Shows image preview
- Sends image to backend
- Displays prediction and confidence

        ↓

Python Backend
- Receives image
- Preprocesses image
- Loads final_model.keras
- Runs prediction
- Returns result as JSON

        ↓

Saved Model
- final_model.keras
- class_names.json

flask = python framework which will be used to create web servers and APIs

need to use flask since react wont be able to load and run the tf Model

test for front and backend connected:
1. test success for image getting chosen and predicted and everything showing ✓
2. click classify without choosing an image, should result in error message ✓
3. upload a non image file returns an error message ✓
4. tried to classify when backend is not running should return failed to fetch ✓
5. upload an image file that is far too large, return an error message about it ✓

need to implement: 
"help make the model better" pin 

main prio:
acne tracker 
- calendar
- food intake
- workout time to shower time 
- sleep
- products used on face - skincare
- whatever else an acne tracker has 


add images to about page, drawn images black and white with red pin

most recent:
fix calendar buttons make a little wider 
maybe split two containers so the the main glass one doesnt expand
        so have that there are two glass outlines instead of one big one that contains both containers 


future:
add a dashboard that can give you an analysis from how long you have been tracking, almost like a stock portfolio, maybe june to sept acne was high, sept to dec still a lot of breakouts 
